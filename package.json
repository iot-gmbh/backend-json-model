{
  "name": "project-planning",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@azure/msal-node": "^1.0.1",
    "@microsoft/microsoft-graph-client": "^2.2.1",
    "@sap/cds": "^4.6.4",
    "@sap/hana-client": "^2.7.23",
    "@sapmentors/cds-scp-api": "github:sapmentors/cds-scp-api",
    "azure-devops-node-api": "^10.2.1",
    "cdse": "^1.1.1",
    "dotenv": "^8.2.0",
    "express": "^4",
    "isomorphic-fetch": "^3.0.0",
    "sap-cf-axios": "^0.2.29"
  },
  "devDependencies": {
    "sqlite3": "^5.0.2"
  },
  "scripts": {
    "start": "cds run",
    "build": "mbt build",
    "deploy": "npm run build && cf deploy mta_archives/%npm_package_name%_%npm_package_version%.mtar"
  },
  "files": [
    "db",
    "srv",
    "app"
  ],
  "cds": {
    "requires": {
      "db": {
        "kind": "sql"
      }
    }
  }
}
