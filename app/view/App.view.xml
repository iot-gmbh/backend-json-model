<mvc:View controllerName="iot.planner.controller.App"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m">

    <!-- Make it 99% to prevent ugly 'double' scroll-bars -->
    <Toolbar >
        <Button icon="sap-icon://nav-back" press="onPressNavBack"/>
        <Title text="Project Planning"/>
        <ToolbarSpacer ></ToolbarSpacer>
        <!-- <Text text="{session>/account/username}" visible="{= !!${session>/account}}"/>
        <Button text="Login" visible="{= !${session>/account}}" press="login"/>
        <Button text="Logout" visible="{= !!${session>/account}}" press="logout"/> -->
        <Button icon="sap-icon://refresh" press="onPressRefreshPage"/>
    </Toolbar>
    <App busyIndicatorDelay="0" id="app" height="{viewModel>/heightWithoutHeaderToolbar}" class="sapUiSizeCompact"/>
    <!-- <App autoFocus="false" height="99.5%">
        <Page enableScrolling="false" title="Project Planning" showNavButton="false" showFooter="false">
            <customHeader>
            </customHeader>
            <NavContainer id="app" autoFocus="false">
            </NavContainer>
        </Page>

    </App> -->
</mvc:View>
