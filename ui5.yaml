specVersion: "2.6"
type: module
metadata:
  name: backend-json-model
builder:
  customTasks:
    - name: ui5-tooling-modules-task
      afterTask: replaceVersion
server:
  customMiddleware:
    - name: ui5-tooling-modules-middleware
      afterMiddleware: compression
resources:
  configuration:
    paths:
      "/resources/iot/": "./" # note the trailing slash!
